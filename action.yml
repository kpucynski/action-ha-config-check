name: 'Home-Assistant Config Check'
description: 'Check HA config'
inputs:
  ha_version:
    description: 'HA version to use'
    required: false
    default: 'latest'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.ha_version }}
